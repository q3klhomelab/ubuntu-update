[project]
name = "ubuntu-update"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "dotenv>=0.9.9",
    "requests>=2.34.2",
]

[project.scripts]
ubuntu-update = "ubuntu_update:main"

[build-system]
requires = ["uv_build>=0.12.8,<0.13.0"]
build-backend = "uv_build"
